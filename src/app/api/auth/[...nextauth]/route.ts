import { handlers } from "@/app/auth"; // Referring to the auth.ts we just created
export const { GET, POST } = handlers;

///api/auth/signin default path to access login form by default given by auth.js library
